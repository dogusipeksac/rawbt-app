# Görsel Diyagramlar ve Akış Şemaları

## 🏗️ Mimari Diyagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI LAYER                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    MainActivity.kt                        │  │
│  │  - Activity Lifecycle                                     │  │
│  │  - ViewModel Instance                                     │  │
│  │  - Compose Setup                                          │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                           │                                     │
│  ┌────────────────────────▼─────────────────────────────────┐  │
│  │                   PrinterScreen.kt                        │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ ConnectionSettingsCard                              │ │  │
│  │  │  - IP Address Input                                 │ │  │
│  │  │  - Port Input                                       │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ PrintTextCard                                       │ │  │
│  │  │  - Multi-line Text Input                            │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ PrintButtonsCard                                    │ │  │
│  │  │  - Test Print                                       │ │  │
│  │  │  - Print Custom Text                                │ │  │
│  │  │  - Print Sample Receipt                             │ │  │
│  │  │  - Print Demo                                       │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ LoadingIndicator & InfoCard                         │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              │ State Flow
                              │ User Actions
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                      VIEWMODEL LAYER                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 PrinterViewModel.kt                       │  │
│  │                                                           │  │
│  │  State Management:                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ PrinterUiState                                     │  │  │
│  │  │  - ipAddress: String                               │  │  │
│  │  │  - port: String                                    │  │  │
│  │  │  - printText: String                               │  │  │
│  │  │  - isLoading: Boolean                              │  │  │
│  │  │  - message: String?                                │  │  │
│  │  │  - isError: Boolean                                │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Business Logic:                                          │  │
│  │  - updateIpAddress()                                      │  │
│  │  - updatePort()                                           │  │
│  │  - updatePrintText()                                      │  │
│  │  - printTest()                                            │  │
│  │  - printCustomText()                                      │  │
│  │  - printSampleReceipt()                                   │  │
│  │  - printDemo()                                            │  │
│  │                                                           │  │
│  │  Coroutine Scope: viewModelScope                         │  │
│  └────────────────────────┬─────────────────────────────────┘  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              │ Repository Calls
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                     REPOSITORY LAYER                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                PrinterRepository.kt                       │  │
│  │                                                           │  │
│  │  Data Operations:                                         │  │
│  │  - printCustomText(ip, port, text)                        │  │
│  │  - printTest(ip, port)                                    │  │
│  │  - printSampleReceipt(ip, port)                           │  │
│  │  - printDemo(ip, port)                                    │  │
│  │                                                           │  │
│  │  ESC/POS Command Building:                                │  │
│  │  - buildEscPosCommand { ... }                             │  │
│  │                                                           │  │
│  │  Dependencies:                                            │  │
│  │  - PrinterClient                                          │  │
│  └────────────────────────┬─────────────────────────────────┘  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              │ Print Commands
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                        DATA LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  PrinterClient.kt                         │  │
│  │                                                           │  │
│  │  TCP Socket Operations:                                   │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ print(ip, port, data)                              │  │  │
│  │  │  1. Validate IP & Port                             │  │  │
│  │  │  2. Create Socket                                  │  │  │
│  │  │  3. Connect with Timeout                           │  │  │
│  │  │  4. Get OutputStream                               │  │  │
│  │  │  5. Write Data                                     │  │  │
│  │  │  6. Flush                                          │  │  │
│  │  │  7. Close Socket                                   │  │  │
│  │  │  8. Return PrintResult                             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Error Handling:                                          │  │
│  │  - SocketTimeoutException                                 │  │
│  │  - UnknownHostException                                   │  │
│  │  - ConnectException                                       │  │
│  │  - IOException                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 EscPosCommands.kt                         │  │
│  │                                                           │  │
│  │  Command Builder:                                         │  │
│  │  - initialize()                                           │  │
│  │  - text() / textLine()                                    │  │
│  │  - boldText() / boldTextLine()                            │  │
│  │  - doubleText() / doubleTextLine()                        │  │
│  │  - alignLeft/Center/Right()                               │  │
│  │  - horizontalLine()                                       │  │
│  │  - newLine()                                              │  │
│  │  - twoColumnText()                                        │  │
│  │  - feedPaper()                                            │  │
│  │  - cutPaper()                                             │  │
│  │                                                           │  │
│  │  ESC/POS Constants:                                       │  │
│  │  - INIT, ALIGN_*, BOLD_*, DOUBLE_*, etc.                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              │ TCP/IP
                              │
                    ┌─────────▼──────────┐
                    │   THERMAL PRINTER  │
                    │   (ESC/POS)        │
                    └────────────────────┘
```

## 🔄 Veri Akış Diyagramı

### Yazdırma İşlemi Akışı

```
┌──────────────┐
│     USER     │
└──────┬───────┘
       │ 1. Button Click
       │
┌──────▼────────────────────────────────────────────────────────┐
│                    PrinterScreen                              │
│  - Captures user action                                       │
│  - Calls ViewModel function                                   │
└──────┬────────────────────────────────────────────────────────┘
       │ 2. viewModel.printTest()
       │
┌──────▼────────────────────────────────────────────────────────┐
│                  PrinterViewModel                             │
│  Step 1: Validate Input                                       │
│    if (ipAddress.isBlank()) return error                      │
│                                                                │
│  Step 2: Update State                                         │
│    _uiState.update { it.copy(isLoading = true) }             │
│                                                                │
│  Step 3: Launch Coroutine                                     │
│    viewModelScope.launch { ... }                              │
└──────┬────────────────────────────────────────────────────────┘
       │ 3. repository.printTest(ip, port)
       │
┌──────▼────────────────────────────────────────────────────────┐
│                 PrinterRepository                             │
│  Step 1: Build ESC/POS Commands                               │
│    val data = buildEscPosCommand {                            │
│      initialize()                                             │
│      alignCenter()                                            │
│      doubleTextLine("TEST")                                   │
│      ...                                                      │
│    }                                                          │
│                                                                │
│  Step 2: Call PrinterClient                                   │
│    return printerClient.print(ip, port, data)                 │
└──────┬────────────────────────────────────────────────────────┘
       │ 4. printerClient.print(ip, port, data)
       │
┌──────▼────────────────────────────────────────────────────────┐
│                   PrinterClient                               │
│  Step 1: Validate                                             │
│    - Check IP format                                          │
│    - Check port range                                         │
│    - Check data not empty                                     │
│                                                                │
│  Step 2: Create Socket                                        │
│    socket = Socket()                                          │
│    socket.connect(InetSocketAddress(ip, port), 5000)         │
│                                                                │
│  Step 3: Send Data                                            │
│    outputStream = socket.getOutputStream()                    │
│    outputStream.write(data)                                   │
│    outputStream.flush()                                       │
│                                                                │
│  Step 4: Cleanup                                              │
│    outputStream.close()                                       │
│    socket.close()                                             │
│                                                                │
│  Step 5: Return Result                                        │
│    return PrintResult.Success("Başarılı")                     │
└──────┬────────────────────────────────────────────────────────┘
       │ 5. PrintResult
       │
┌──────▼────────────────────────────────────────────────────────┐
│                 PrinterRepository                             │
│  - Receives PrintResult                                       │
│  - Returns to ViewModel                                       │
└──────┬────────────────────────────────────────────────────────┘
       │ 6. PrintResult
       │
┌──────▼────────────────────────────────────────────────────────┐
│                  PrinterViewModel                             │
│  Step 1: Handle Result                                        │
│    when (result) {                                            │
│      is Success -> update state with success message          │
│      is Error -> update state with error message              │
│    }                                                          │
│                                                                │
│  Step 2: Update State                                         │
│    _uiState.update {                                          │
│      it.copy(                                                 │
│        isLoading = false,                                     │
│        message = result.message,                              │
│        isError = result is Error                              │
│      )                                                        │
│    }                                                          │
└──────┬────────────────────────────────────────────────────────┘
       │ 7. StateFlow Emission
       │
┌──────▼────────────────────────────────────────────────────────┐
│                    PrinterScreen                              │
│  Step 1: Collect State                                        │
│    val uiState by viewModel.uiState.collectAsStateWithLifecycle()│
│                                                                │
│  Step 2: Recompose                                            │
│    - Hide loading indicator                                   │
│    - Show snackbar with message                               │
│    - Enable buttons                                           │
└──────┬────────────────────────────────────────────────────────┘
       │ 8. UI Update
       │
┌──────▼───────┐
│     USER     │
│  Sees Result │
└──────────────┘
```

## 🔌 TCP Socket Bağlantı Akışı

```
┌─────────────────────────────────────────────────────────────┐
│                    PrinterClient.print()                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                ┌─────────▼──────────┐
                │  Validate Input    │
                │  - IP Address      │
                │  - Port Number     │
                │  - Data Size       │
                └─────────┬──────────┘
                          │
                    ┌─────▼──────┐
                    │   Valid?   │
                    └─────┬──────┘
                          │
              ┌───────────┴───────────┐
              │ NO                YES │
              ▼                       ▼
    ┌─────────────────┐    ┌──────────────────┐
    │ Return Error    │    │ Create Socket    │
    │ "Invalid Input" │    │ socket = Socket()│
    └─────────────────┘    └────────┬─────────┘
                                    │
                          ┌─────────▼──────────┐
                          │ Connect to Printer │
                          │ Timeout: 5 seconds │
                          └─────────┬──────────┘
                                    │
                              ┌─────▼──────┐
                              │ Connected? │
                              └─────┬──────┘
                                    │
                        ┌───────────┴───────────┐
                        │ NO                YES │
                        ▼                       ▼
              ┌──────────────────┐   ┌──────────────────┐
              │ Catch Exception  │   │ Get OutputStream │
              │ - Timeout        │   └────────┬─────────┘
              │ - Unknown Host   │            │
              │ - Connect Error  │   ┌────────▼─────────┐
              └────────┬─────────┘   │ Write Data       │
                       │             │ outputStream     │
                       │             │   .write(data)   │
                       │             └────────┬─────────┘
                       │                      │
                       │             ┌────────▼─────────┐
                       │             │ Flush Stream     │
                       │             │ outputStream     │
                       │             │   .flush()       │
                       │             └────────┬─────────┘
                       │                      │
                       │             ┌────────▼─────────┐
                       │             │ Close Resources  │
                       │             │ - outputStream   │
                       │             │ - socket         │
                       │             └────────┬─────────┘
                       │                      │
                       ▼                      ▼
              ┌──────────────────┐   ┌──────────────────┐
              │ PrintResult      │   │ PrintResult      │
              │   .Error()       │   │   .Success()     │
              └──────────────────┘   └──────────────────┘
```

## 📊 State Management Diyagramı

```
┌─────────────────────────────────────────────────────────────┐
│                      ViewModel                              │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │         Private Mutable State                         │ │
│  │  _uiState = MutableStateFlow(PrinterUiState())       │ │
│  └─────────────────────┬─────────────────────────────────┘ │
│                        │                                    │
│                        │ .asStateFlow()                     │
│                        │                                    │
│  ┌─────────────────────▼─────────────────────────────────┐ │
│  │         Public Immutable State                        │ │
│  │  uiState: StateFlow<PrinterUiState>                  │ │
│  └─────────────────────┬─────────────────────────────────┘ │
└────────────────────────┼───────────────────────────────────┘
                         │
                         │ Emission
                         │
┌────────────────────────▼───────────────────────────────────┐
│                    Compose UI                              │
│                                                             │
│  val uiState by viewModel.uiState                          │
│                   .collectAsStateWithLifecycle()           │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  State Changes Trigger Recomposition                 │ │
│  │                                                       │ │
│  │  if (uiState.isLoading) {                            │ │
│  │    CircularProgressIndicator()                       │ │
│  │  }                                                    │ │
│  │                                                       │ │
│  │  TextField(value = uiState.ipAddress)                │ │
│  │                                                       │ │
│  │  Button(enabled = !uiState.isLoading)                │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  User Actions                                        │ │
│  │                                                       │ │
│  │  onClick = { viewModel.printTest() }                 │ │
│  │  onValueChange = { viewModel.updateIpAddress(it) }   │ │
│  └────────────────────┬─────────────────────────────────┘ │
└─────────────────────────┼───────────────────────────────────┘
                          │
                          │ Action
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                      ViewModel                              │
│                                                             │
│  fun updateIpAddress(ip: String) {                          │
│    _uiState.update { it.copy(ipAddress = ip) }            │
│  }                                                          │
│                                                             │
│  fun printTest() {                                          │
│    viewModelScope.launch {                                  │
│      _uiState.update { it.copy(isLoading = true) }        │
│      val result = repository.printTest(...)                 │
│      _uiState.update {                                     │
│        it.copy(                                            │
│          isLoading = false,                                │
│          message = result.message                          │
│        )                                                    │
│      }                                                      │
│    }                                                        │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
```

## 🎨 UI Component Hierarchy

```
MainActivity
    └── RawBTAppTheme
            └── PrinterScreen
                    ├── Scaffold
                    │   ├── TopAppBar
                    │   │   └── Text("WiFi Termal Yazıcı")
                    │   │
                    │   ├── SnackbarHost
                    │   │
                    │   └── Content
                    │       └── Column (Scrollable)
                    │           │
                    │           ├── ConnectionSettingsCard
                    │           │   └── Card
                    │           │       └── Column
                    │           │           ├── Text("Bağlantı Ayarları")
                    │           │           ├── OutlinedTextField (IP)
                    │           │           └── OutlinedTextField (Port)
                    │           │
                    │           ├── PrintTextCard
                    │           │   └── Card
                    │           │       └── Column
                    │           │           ├── Text("Yazdırılacak Metin")
                    │           │           └── OutlinedTextField (Multi-line)
                    │           │
                    │           ├── PrintButtonsCard
                    │           │   └── Card
                    │           │       └── Column
                    │           │           ├── Text("Yazdırma İşlemleri")
                    │           │           ├── Button("Test Yazdır")
                    │           │           ├── Button("Metin Yazdır")
                    │           │           ├── OutlinedButton("Örnek Fiş")
                    │           │           └── OutlinedButton("Demo")
                    │           │
                    │           ├── LoadingIndicator (Conditional)
                    │           │   └── Card
                    │           │       └── Row
                    │           │           ├── CircularProgressIndicator
                    │           │           └── Text("Bağlanılıyor...")
                    │           │
                    │           └── InfoCard
                    │               └── Card
                    │                   └── Column
                    │                       ├── Text("ℹ️ Kullanım Bilgisi")
                    │                       └── Text (Multiple tips)
```

## 🔄 Coroutine Scope Diyagramı

```
┌─────────────────────────────────────────────────────────────┐
│                      MainActivity                           │
│  - lifecycleScope (Activity scope)                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   PrinterViewModel                          │
│                                                             │
│  viewModelScope.launch {                                    │
│    │                                                        │
│    ├─ Main Thread (Dispatchers.Main)                       │
│    │  - Update UI State                                    │
│    │  - Validation                                         │
│    │                                                        │
│    └─ withContext(Dispatchers.IO) {                        │
│       │                                                     │
│       └─ IO Thread                                          │
│          - Network Operations                               │
│          - Socket Connection                                │
│          - Data Transmission                                │
│    }                                                        │
│    │                                                        │
│    └─ Back to Main Thread                                  │
│       - Handle Result                                       │
│       - Update UI State                                     │
│  }                                                          │
│                                                             │
│  Automatic Cancellation:                                    │
│  - When ViewModel is cleared                                │
│  - When Activity is destroyed                               │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Dependency Graph

```
MainActivity
    │
    ├─ depends on ─> PrinterViewModel
    │                    │
    │                    ├─ depends on ─> PrinterRepository
    │                    │                    │
    │                    │                    ├─ depends on ─> PrinterClient
    │                    │                    │
    │                    │                    └─ depends on ─> EscPosCommands
    │                    │
    │                    └─ depends on ─> Kotlin Coroutines
    │
    └─ depends on ─> PrinterScreen
                         │
                         └─ depends on ─> Jetpack Compose
```

## 🎯 Sonuç

Bu diyagramlar, uygulamanın mimarisini, veri akışını ve bileşenler arası ilişkileri görsel olarak gösterir. Her diyagram, sistemin farklı bir yönünü açıklar ve geliştirme sürecinde referans olarak kullanılabilir.
